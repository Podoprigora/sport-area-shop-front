import React from 'react';
import PropTypes from 'prop-types';
import { Page } from '@components/Page';

const FavoritesPage = (props) => {
    return <Page>Favorites</Page>;
};

FavoritesPage.propTypes = {};

export default FavoritesPage;
