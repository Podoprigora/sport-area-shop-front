import Search from '@resources/icons/icomoon-feather/search.svg';
import withIconAttributes from '../withSvgAttrStyles';

const SearchIcon = withIconAttributes(Search);

export default SearchIcon;
