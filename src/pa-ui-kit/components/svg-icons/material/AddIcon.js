import Add from '../../../assets/svg-icons/icomoon-material/add.svg';
import withIconAttributes from '../../withSvgIconAttributes';

const AddIcon = withIconAttributes(Add);

export default AddIcon;
