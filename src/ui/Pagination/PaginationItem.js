import React from 'react';
import PropTypes from 'prop-types';

const PaginationItem = (props) => {
    return <div className="pagination__item">null</div>;
};

PaginationItem.propTypes = {};

export default PaginationItem;
