import Filter from '@resources/icons/icomoon-feather/filter.svg';
import withIconAttributes from '../withSvgAttrStyles';

const FilterIcon = withIconAttributes(Filter);

export default FilterIcon;
